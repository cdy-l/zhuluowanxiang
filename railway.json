{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update -qq && apt-get install -y -qq nodejs && npm install"
  },
  "deploy": {
    "startCommand": "python3 main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
